# Inbound address is issued on Telegram

Status: accepted. Extends ADR-0003.

Each **member** still gets a unique **inbound address**. Because there is no dashboard in this slice, Gravy Scout sends that address and forwarding instructions in Telegram after the **career profile** is usable. The address is a **connection**, created from the talk **surface**, not from `/app`.
